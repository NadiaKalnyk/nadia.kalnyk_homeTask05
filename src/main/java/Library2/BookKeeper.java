/*package Library2;

import java.awt.print.Book;

public class BookKeeper{
    private final Library2 library;

    public BookKeeper() {
        this.library = new Library2();
        library.loadBookToLibrary();
    }
    public Book2 getBookByAuthor(String author){
        //return

        for (Book2 book: library.getBook2()) {
            if (book.getAuthor().equals(author)) {
                return book;
            }
        }
        return null;
    }
    public Book2 getBookByTitle(String title){
        //return

        for (Book2 book: library.getBook2()) {
            if (book.getTitle().equals(title)) {
                return book;
            }
        }
        return null;
    }
}
*/